[build.environment]
  NPM_FLAGS = "--prefix=/dev/null"
  NODE_VERSION = "16"

[build]
  publish = "dist"
  command = "npx pnpm i && npx pnpm run build:demo"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
